@require "../styles/colors"
@require "../styles/variables"

.archive
  wrapper-padding()

  ul
    .metadata
      margin-top -0.7vh
      margin-bottom 0.2vh
      .timestamp
        padding 0
        font-style italic
        color rgba($color-blueGrey500, .5)
        border none
        background: none
        .timePart
          display inline
